import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  // experimental: {
  //   cacheComponents: true  // PPR is now enabled via cacheComponents in Next.js 16 canary
  // }
};

export default nextConfig;
